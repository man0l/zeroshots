# Required for email waitlist signup. Without these, signups are only logged.
# MailerLite: use base URL (we append /subscribers) or full URL:
MAILER_API_KEY=your-mailer-api-key-here
MAILER_API_ENDPOINT=https://connect.mailerlite.com/api