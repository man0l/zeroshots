name: Android Build

on:
  push:
    branches: [main, enet_analytics]
    paths:
      - 'screenshot-organizer/src/**'
      - 'screenshot-organizer/app/**'
      - 'screenshot-organizer/assets/**'
      - 'screenshot-organizer/package.json'
      - 'screenshot-organizer/package-lock.json'
      - 'screenshot-organizer/tsconfig.json'
      - 'screenshot-organizer/app.json'
      - '.github/workflows/android-build.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: screenshot-organizer

permissions:
  contents: write

jobs:
  build:
    name: Release APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: screenshot-organizer/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Get app version
        id: version
        run: |
          VERSION=$(node -p "require('./app.json').expo.version")
          echo "app_version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Write .env for production build
        run: |
          cat > .env <<EOF
          EXPO_PUBLIC_SUPABASE_URL=${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
          EXPO_PUBLIC_REVENUECAT_API_KEY=${{ secrets.EXPO_PUBLIC_REVENUECAT_API_KEY }}
          EXPO_PUBLIC_EDGE_FUNCTION_PREFIX=${{ secrets.EXPO_PUBLIC_EDGE_FUNCTION_PREFIX }}
          EOF

      - name: Expo prebuild
        run: npx expo prebuild --platform android --no-install

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}

      - name: Build release APK (arm64 only)
        working-directory: screenshot-organizer/android
        run: >
          ./gradlew assembleRelease
          -PreactNativeArchitectures=arm64-v8a
          -Dorg.gradle.jvmargs=-Xmx4g
          -Dorg.gradle.parallel=true
          --build-cache
          --no-daemon
          -x lint
          -x test

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: screenshot-organizer/android/app/build/outputs/apk/release/app-release.apk
          retention-days: 7

      - name: Create tag and release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: ${{ github.workspace }}
        run: |
          TAG="android-v${{ steps.version.outputs.app_version }}-build.${{ github.run_number }}"
          SHORT_SHA="${GITHUB_SHA::7}"
          APK_PATH="screenshot-organizer/android/app/build/outputs/apk/release/app-release.apk"

          gh release create "$TAG" \
            "$APK_PATH#screenshot-organizer-release.apk" \
            --title "Android Release Build #${{ github.run_number }}" \
            --notes "**Version:** ${{ steps.version.outputs.app_version }}
          **Commit:** [\`${SHORT_SHA}\`](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
          **Branch:** \`${{ github.ref_name }}\`
          **Architecture:** arm64-v8a

          Built from workflow run [#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" \
            --prerelease
